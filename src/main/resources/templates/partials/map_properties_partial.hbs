<div class="card">
    <div class="card-body">
        <h4 class="card-title">Properties</h4>
        {{#each this.properties}}
            <button type="button" class="btn btn-filter btn-outline-default waves-effect"
                    onclick="toggle_filter_button_state(this)">{{name}}</button>
        {{/each}}
    </div>
</div>