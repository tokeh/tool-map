<div id="tool-map" class="col-md-12">
    <div class="row">
        <h3>Tool Map</h3>
    </div>

    <div class="row row-map-title">
        <div class="col-md-2 d-none d-md-block">
            <h5>Tool Name</h5>
        </div>
        {{#each this.dimensions}}
            <div class="col-md d-none d-md-block"><h5>{{name}}</h5></div>
        {{/each}}
    </div>

    {{#each this.tools}}
        <div class="row">
            <div class="col-md-2 d-none d-md-block card map-card">
                <div class="card-body ">
                    <div class="card-title">
                        <h5>{{toolName}}</h5>
                    </div>
                </div>
            </div>

            <div class="col-md-10 d-none d-md-block card map-card">
                <div class="card-body">
                    <div class="row">
                        {{#each mapView}}
                            <div class="col-md">
                                {{#each this}}
                                    {{name}}
                                {{/each}}
                            </div>
                        {{/each}}
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-sm-12 d-sm-block d-md-none card map-card">
                <div class="card-body ">
                    <div class="card-title">
                        <h4>{{toolName}}</h4>
                    </div>
                    {{#each mapView}}
                        {{#each this}}
                            <p>
                                {{name}}
                            </p>
                        {{/each}}
                    {{/each}}
                </div>
            </div>
        </div>
    {{/each}}
</div>
